meta {
  name: get player bans
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/servers/{{serverID}}/players/{{steamID64}}/bans
  body: none
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}
