meta {
  name: get player bans
  type: http
  seq: 2
}

get {
  url: {{api_url}}/{{api_version}}/servers/:serverID/players/:steamID64/bans
  body: none
  auth: bearer
}

auth:bearer {
  token: {{api_server_token}}
}
