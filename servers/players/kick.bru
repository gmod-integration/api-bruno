meta {
  name: kick
  type: http
  seq: 4
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/players/{{steamID64}}/kick
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}
