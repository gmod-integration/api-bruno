meta {
  name: changeName
  type: http
  seq: 11
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/players/{{steamID64}}/changeName
  body: none
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}
