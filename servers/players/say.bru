meta {
  name: say
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/players/{{steamID64}}/say
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}

body:json {
  {
      "player": {{player}},
      "text": "Hello World",
      "teamOnly": false
  }
}
