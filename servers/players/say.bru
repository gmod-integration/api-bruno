meta {
  name: say
  type: http
  seq: 3
}

post {
  url: {{baseURL}}/user/say
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}

body:json {
  {
      "steamID64": "{{steamID64}}",
      "message": "Hello World",
      "name": "{{user_name}}"
  }
}
