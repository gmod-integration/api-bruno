meta {
  name: unBan
  type: http
  seq: 8
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/players/{{steamID64}}/unBan
  body: none
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}
