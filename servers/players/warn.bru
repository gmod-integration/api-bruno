meta {
  name: warn
  type: http
  seq: 5
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/players/{{steamID64}}/warn
  body: none
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}
