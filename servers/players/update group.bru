meta {
  name: update group
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/players/{{steamID64}}/group
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}

body:json {
  {
      "player": {{player}},
      "oldGroup": "vip",
      "newGroup": "user"
  }
  
}
