meta {
  name: unWarn
  type: http
  seq: 6
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/players/{{steamID64}}/unWarn
  body: none
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}
