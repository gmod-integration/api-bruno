meta {
  name: ban
  type: http
  seq: 7
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/players/{{steamID64}}/ban
  body: none
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}
