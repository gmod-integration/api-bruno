meta {
  name: send message
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/channels/{{channelID}}/messages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}

body:json {
  {
    "content": "Hello World"
  }
}
