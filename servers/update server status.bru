meta {
  name: update server status
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/{{apiVersion}}/servers/{{serverID}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{serverToken}}
}

body:json {
  {{server}}
}
