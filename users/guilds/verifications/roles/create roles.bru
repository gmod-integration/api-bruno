meta {
  name: create roles
  type: http
  seq: 4
}

post {
  url: {{baseURL}}/{{apiVersion}}/users/{{discordID}}/guilds/{{guildID}}/verifications/roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}
