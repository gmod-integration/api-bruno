meta {
  name: get settings
  type: http
  seq: 1
}

get {
  url: {{baseURL}}/{{apiVersion}}/users/{{discordID}}/guilds/{{guildID}}/servers/{{serverID}}/settings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}
