meta {
  name: find status
  type: http
  seq: 1
}

get {
  url: {{baseURL}}/{{apiVersion}}/users/{{discordID}}/guilds/{{guildID}}/servers/{{serverID}}/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}
