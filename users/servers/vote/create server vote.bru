meta {
  name: create server vote
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/{{apiVersion}}/users/{{discordID}}/servers/{{serverID}}/vote
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}
